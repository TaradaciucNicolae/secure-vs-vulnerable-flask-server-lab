services:
  flask:
    build: .
    container_name: SW_Server_With_Vulnerabilities
    env_file:
      - .env
    volumes:
      - .:/SW_Server_With_Vulnerabilities
    ports:
      - "${FLASK_PORT}:${FLASK_PORT}"
    restart: always